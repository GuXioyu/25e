# Ignore everything by default.
*

# Keep directories reachable so nested source files can be unignored.
!*/

# Keep C source and header files.
!*.c
!*.h
!*.py
!*.uvprojx

# Keep this ignore file.
!.gitignore
